Fixture data is used for dev, testing and staging evironments
For dev and staging use the bootstrap_db manager. In testing fixtures are 
referenced in test class

# Files
*dev.json used for staging and dev
    *composed of 
        *base.json
        *core_user.json
        *alarms.json
The remainder of the files are used as test fixtures


After edits save

./mg.sh 'dumpdata --indent=2 > fixtures/dev.json'


append to all to all.json



